import React from 'react'

const Buttons = ({ item }) => {
    return (
        <div className='bg-gray-100 py-2 px-3 m-2 rounded-lg whitespace-nowrap cursor-pointer'>{item}</div>
    )
}

export default Buttons